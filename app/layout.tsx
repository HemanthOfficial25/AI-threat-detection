import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "DRONNO TECH | Hybrid Rover-Drone Intelligence",
  description: "Integrated Rover–Drone Hybrid System for Search & Rescue and Precision Agriculture. Hybrid Rover-Drone Intelligence by DRONNO TECH.",
  keywords: ["DRONNO TECH", "Robotics", "Drone", "Rover", "Search and Rescue", "Precision Agriculture", "Hybrid Intelligence"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
      </body>
    </html>
  );
}
